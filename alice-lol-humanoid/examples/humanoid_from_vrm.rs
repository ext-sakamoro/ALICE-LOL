//! Placeholder for `humanoid_from_vrm` example (Phase β pending)
//!
//! `alice-lol-humanoid` Cargo.toml に `[[example]] humanoid_from_vrm` が指定されているが
//! 実装 file は Phase β (VRM import 系) 未完成のため placeholder として存在
//!
//! feature `vrm` gated、`cargo run --example humanoid_from_vrm --features vrm` で実行
//! 本 file は最小 stub、実装は次段 sprint で埋める
//!
//! (Phase D 進行のための workspace resolve unblock 目的、2026-08-06)

fn main() {
    println!("humanoid_from_vrm: placeholder (Phase β 未実装)");
    println!("Cargo.toml で [[example]] 指定されているが実装は VRM import 系の次段 sprint 予定");
}
